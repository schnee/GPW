package name.schnee.gpw;

public class Permutor {
	
	public static CharSequence replaceLetters(CharSequence input) {
		
		
		//Want to replace characters with numbers and special chars
		//Want to guarantee at least one number and one special char
		
		boolean numberSwitch = false;
		
		for(int i = 0; i < input.length(); i++){
			char candidate = input.charAt(i);
		}
		
		return input;
	}

}
